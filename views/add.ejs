<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/halfmoon@1.1.1/css/halfmoon-variables.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Add page</title>
</head>

<body>

    <form method="post" action="/add">
        <div class="mb-3">
            <label for="firstName" class="form-label">Employee's First Name</label>
            <input type="text" class="form-control" id="firstName" placeholder="Employee's First Name" required="">
        </div>
        <div class="mb-3">
            <label for="lastName" class="form-label">Employee's Last Name </label>
            <input type="text" class="form-control" id="lastName" placeholder="Employee's Last Name" required="">
        </div>

        <div class="mb-3">
            <label for="employeeNumber" class="form-label">Employee's ID</label>
            <input type="text" class="form-control" id="employeeID" placeholder="Employee's ID" required="">
        </div>

        <select class="form-select" aria-label="Default select example">
                <option selected>Select Job Title</option>
                <% roles.forEach((role)=> { %>
                <option value="<%=role.role_id%>">
                    <%=role.role%>
                </option>
        </select>
        <% }) %>

        <div class="mb-3">
            <label for="employeeNumber" class="form-label">Employee's salary</label>
            <input type="number" class="form-control" id="employeeSalary" placeholder="Employee's salary"
                required="">
        </div>

        <div class="mb-3">
            <label for="address" class="form-label">Employee's Address</label>
            <input type="text" class="form-control" id="firstName" placeholder="Employee's Address" required="">
        </div> 
        <button type="submit" class="btn btn-primary">Submit</button>
    </form>
</body>

</html>